@model IEnumerable<TapHoa.Data.Khuyenmai>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Login.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Phantramgiam)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Phantramgiam)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Makm">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Makm">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Makm">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
